<?xml version="1.0" encoding="utf-8"?>
<!-- Square-cornered variant of the W'bal field background, used when the rider has disabled
     field boundaries (no rounded edge to clip against). Solid white so it can be tinted to the
     status color at runtime via setBackgroundTintList. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="#FFFFFFFF" />

</shape>
